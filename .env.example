# ── Backend (NAS_base) ────────────────────────────────────────────────────────
DB_HOST=mariadb
DB_PORT=3306
DB_NAME=drupal
DB_USER=drupal
DB_PASSWORD=
DB_ROOT_PASSWORD=
DRUPAL_ROOT=./NAS_base/drupal

# ── Frontend (NAS_frontend) ───────────────────────────────────────────────────
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
